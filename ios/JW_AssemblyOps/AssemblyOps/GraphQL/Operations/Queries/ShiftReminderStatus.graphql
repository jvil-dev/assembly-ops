query ShiftReminderStatus($shiftId: ID!) {
  shiftReminderStatus(shiftId: $shiftId) {
    shiftId
    shiftName
    totalAssigned
    totalConfirmed
    confirmations {
      eventVolunteerId
      firstName
      lastName
      confirmed
      confirmedAt
    }
  }
}
