query AVHazardAssessments($eventId: ID!) {
  avHazardAssessments(eventId: $eventId) {
    id
    title
    hazardType
    description
    controls
    ppeRequired
    completedBy {
      id
      firstName
      lastName
    }
    session {
      id
      name
    }
    completedAt
  }
}
