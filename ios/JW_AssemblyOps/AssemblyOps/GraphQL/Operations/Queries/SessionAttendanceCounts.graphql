query SessionAttendanceCounts($sessionId: ID!) {
  sessionAttendanceCounts(sessionId: $sessionId) {
    id
    count
    notes
    session {
      id
      name
    }
    submittedBy {
      id
      firstName
      lastName
    }
    createdAt
  }
}
