query MyNotifications($eventId: ID!, $limit: Int, $offset: Int) {
  myNotifications(eventId: $eventId, limit: $limit, offset: $offset) {
    id
    type
    title
    body
    data
    isRead
    createdAt
  }
}
