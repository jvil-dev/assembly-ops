query CaptainVolunteers($eventId: ID!, $departmentId: ID!) {
  captainVolunteers(eventId: $eventId, departmentId: $departmentId) {
    id
    user {
      id
      firstName
      lastName
    }
  }
}
