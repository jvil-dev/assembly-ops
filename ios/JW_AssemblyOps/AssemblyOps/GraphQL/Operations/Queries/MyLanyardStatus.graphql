query MyLanyardStatus($eventId: ID!, $date: String) {
  myLanyardStatus(eventId: $eventId, date: $date) {
    id
    eventVolunteerId
    eventId
    date
    pickedUpAt
    returnedAt
    volunteerName
  }
}
