query MyAllEvents {
  myAllEvents {
    eventId
    membershipType
    overseerRole
    departmentId
    departmentName
    departmentType
    eventVolunteerId
    departmentAccessCode
    event {
      id
      name
      eventType
      theme
      venue
      address
      startDate
      endDate
      volunteerCount
    }
  }
}
