query VolunteerAssignments($volunteerId: ID!) {
    volunteerAssignments(volunteerId: $volunteerId) {
        id
        isCaptain
        status
        forceAssigned
        post {
            id
            name
        }
        session {
            id
            name
            date
        }
    }
}
