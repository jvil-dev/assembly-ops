query EventAdmins($eventId: ID!) {
  eventAdmins(eventId: $eventId) {
    id
    role
    claimedAt
    admin {
      id
      firstName
      lastName
      email
    }
    department {
      id
      name
      departmentType
    }
  }
}
