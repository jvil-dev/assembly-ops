query MyMessages($filter: MessageFilterInput, $limit: Int, $offset: Int) {
    myMessages(filter: $filter, limit: $limit, offset: $offset) {
        id
        subject
        body
        recipientType
        senderType
        senderName
        senderId
        isRead
        readAt
        createdAt
        sender {
            id
            firstName
            lastName
        }
        conversation {
            id
        }
    }
}
