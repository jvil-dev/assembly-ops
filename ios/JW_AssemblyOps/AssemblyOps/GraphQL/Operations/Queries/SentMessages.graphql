query SentMessages($limit: Int, $offset: Int) {
  sentMessages(limit: $limit, offset: $offset) {
    id
    subject
    body
    recipientType
    senderType
    senderName
    isRead
    readAt
    volunteer {
      id
      firstName
      lastName
    }
    createdAt
  }
}
