mutation LoginWithGoogle($input: GoogleAuthInput!) {
    loginWithGoogle(input: $input) {
        admin { id email firstName lastName fullName }
        accessToken
        refreshToken
        expiresIn
        isNewUser
        pendingOAuthToken
        email
        firstName
        lastName
    }
}
