mutation ResolveLostPersonAlert($id: ID!, $resolutionNotes: String!) {
  resolveLostPersonAlert(id: $id, resolutionNotes: $resolutionNotes) {
    id
    resolved
    resolvedAt
    resolutionNotes
  }
}
