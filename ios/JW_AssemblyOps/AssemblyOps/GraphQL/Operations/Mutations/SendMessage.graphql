mutation SendMessage($input: SendMessageInput!) {
  sendMessage(input: $input) {
    id
    subject
    body
    recipientType
    senderType
    senderName
    volunteer {
      id
      firstName
      lastName
    }
    conversation {
      id
    }
    createdAt
  }
}
