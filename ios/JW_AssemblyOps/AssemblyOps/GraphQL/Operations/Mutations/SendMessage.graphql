mutation SendMessage($input: SendMessageInput!) {
  sendMessage(input: $input) {
    id
    subject
    body
    recipientType
    senderType
    senderName
    recipientName
    senderId
    conversation {
      id
    }
    createdAt
  }
}
