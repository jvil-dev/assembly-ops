mutation DenyJoinRequest($requestId: ID!, $reason: String) {
  denyJoinRequest(requestId: $requestId, reason: $reason) {
    id
    eventId
    user {
      id
      firstName
      lastName
    }
    departmentType
    status
    note
    createdAt
    resolvedAt
  }
}
