mutation ResolveSafetyIncident($id: ID!, $resolutionNotes: String) {
  resolveSafetyIncident(id: $id, resolutionNotes: $resolutionNotes) {
    id
    resolved
    resolvedAt
    resolutionNotes
  }
}
