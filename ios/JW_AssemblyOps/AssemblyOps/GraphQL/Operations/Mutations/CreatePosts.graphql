mutation CreatePosts($input: CreatePostsInput!) {
    createPosts(input: $input) {
        id
        name
        description
        location
        category
        sortOrder
        createdAt
    }
}
