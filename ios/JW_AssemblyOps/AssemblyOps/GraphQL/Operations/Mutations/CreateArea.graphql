mutation CreateArea($departmentId: ID!, $input: CreateAreaInput!) {
    createArea(departmentId: $departmentId, input: $input) {
        id
        name
        description
        category
        sortOrder
        startTime
        endTime
        postCount
    }
}
