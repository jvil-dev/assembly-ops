mutation UpdateArea($id: ID!, $input: UpdateAreaInput!) {
    updateArea(id: $id, input: $input) {
        id
        name
        description
        category
        sortOrder
        startTime
        endTime
        postCount
    }
}
