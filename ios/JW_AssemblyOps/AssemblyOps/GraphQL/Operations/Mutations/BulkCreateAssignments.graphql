mutation BulkCreateAssignments($inputs: [CreateAssignmentInput!]!) {
  bulkCreateAssignments(inputs: $inputs) {
    id
    isCheckedIn
    volunteer {
      id
      firstName
      lastName
    }
    post {
      id
      name
    }
    session {
      id
      name
    }
  }
}
