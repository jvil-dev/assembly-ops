mutation SetCanCount($input: SetCanCountInput!) {
    setCanCount(input: $input) {
        id
        canCount
        status
        post {
            id
            name
        }
        session {
            id
            name
        }
    }
}
