mutation CreateAssignment($input: CreateAssignmentInput!) {
  createAssignment(input: $input) {
    assignment {
      id
      isCheckedIn
      volunteer {
        id
        firstName
        lastName
      }
      post {
        id
        name
      }
      session {
        id
        name
      }
    }
    warning
  }
}
