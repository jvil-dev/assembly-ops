mutation AcceptAreaCaptain($input: AcceptAreaCaptainInput!) {
  acceptAreaCaptain(input: $input) {
    id
    status
    respondedAt
    area {
      id
      name
    }
    session {
      id
      name
    }
  }
}
