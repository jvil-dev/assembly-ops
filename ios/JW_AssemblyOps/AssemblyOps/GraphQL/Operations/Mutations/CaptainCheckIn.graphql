mutation CaptainCheckIn($input: CaptainCheckInInput!) {
    captainCheckIn(input: $input) {
        id
        status
        isCaptain
        checkIn {
            id
            status
            checkInTime
            notes
        }
    }
}
