mutation PromoteToAppAdmin($input: PromoteToAppAdminInput!) {
  promoteToAppAdmin(input: $input) {
    id
    role
    claimedAt
    user {
      id
      firstName
      lastName
      email
    }
    event {
      id
      name
    }
    department {
      id
      name
      departmentType
    }
  }
}
