mutation UpdatePostSessionStatus($input: UpdatePostSessionStatusInput!) {
  updatePostSessionStatus(input: $input) {
    id
    post {
      id
      name
    }
    session {
      id
      name
    }
    status
    updatedAt
  }
}
