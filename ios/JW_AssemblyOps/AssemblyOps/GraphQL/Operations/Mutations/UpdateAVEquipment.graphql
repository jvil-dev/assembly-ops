mutation UpdateAVEquipment($id: ID!, $input: UpdateAVEquipmentInput!) {
  updateAVEquipment(id: $id, input: $input) {
    id
    name
    model
    serialNumber
    category
    condition
    location
    notes
    area {
      id
      name
    }
    updatedAt
  }
}
