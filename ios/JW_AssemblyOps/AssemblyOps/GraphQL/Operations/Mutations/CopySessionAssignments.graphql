mutation CopySessionAssignments($input: CopySessionAssignmentsInput!) {
  copySessionAssignments(input: $input) {
    copiedCount
    skippedCount
    skippedVolunteers {
      volunteerName
      postName
      reason
    }
    copiedAreaCaptains
  }
}
