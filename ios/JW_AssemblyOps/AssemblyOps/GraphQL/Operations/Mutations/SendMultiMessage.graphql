mutation SendMultiMessage($input: SendMultiMessageInput!) {
  sendMultiMessage(input: $input) {
    id
    subject
    body
    recipientType
    senderType
    senderName
    recipientName
    createdAt
  }
}
