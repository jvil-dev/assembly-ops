mutation RegisterAdmin($input: RegisterAdminInput!) {
    registerAdmin(input: $input) {
        admin {
            id
            email
            firstName
            lastName
            fullName
        }
        accessToken
        refreshToken
        expiresIn
    }
}
